{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "ceo",
        "admin",
        "user"
      ],
      "default": "user"
    },
    "bio": {
      "type": "string"
    },
    "phone": {
      "type": "string"
    },
    "job_title": {
      "type": "string"
    },
    "profile_photo": {
      "type": "string"
    },
    "ai_provider": {
      "type": "string",
      "enum": [
        "none",
        "grok",
        "claude"
      ],
      "default": "none"
    },
    "ai_api_key": {
      "type": "string"
    },
    "display_name": {
      "type": "string",
      "description": "Editable display name"
    },
    "linkedin": {
      "type": "string"
    },
    "department": {
      "type": "string"
    },
    "location": {
      "type": "string"
    },
    "skills": {
      "type": "array",
      "items": {
        "type": "string"
      }
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "inactive",
        "on_leave"
      ],
      "default": "active"
    },
    "joined_date": {
      "type": "string",
      "format": "date"
    },
    "subscription_status": {
      "type": "string",
      "enum": [
        "active",
        "expired",
        "none"
      ],
      "default": "none",
      "description": "Subscription status"
    },
    "paid_through_date": {
      "type": "string",
      "format": "date",
      "description": "Date until which access is granted (from upfront payment or active subscription)"
    },
    "stripe_customer_id": {
      "type": "string",
      "description": "Optional Stripe customer ID for future webhook integration"
    },
    "failed_login_attempts": {
      "type": "integer",
      "default": 0,
      "description": "Failed login counter for account lockout"
    },
    "account_locked": {
      "type": "boolean",
      "default": false,
      "description": "When true, sign-in is blocked until an admin unlocks"
    },
    "mfa_enabled": {
      "type": "boolean",
      "default": false,
      "description": "Whether TOTP MFA is enabled for this user"
    }
  }
}